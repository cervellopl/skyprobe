<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkyProbe"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SkyProbe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- "Share" from any gallery / file manager. Photos are image/*; RAW and FITS
                 are usually offered as application/octet-stream or one of the types below. -->
            <intent-filter android:label="@string/share_label">
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/fits" />
                <data android:mimeType="image/fits" />
                <data android:mimeType="application/x-fits" />
                <data android:mimeType="image/x-adobe-dng" />
                <data android:mimeType="image/x-dcraw" />
                <data android:mimeType="image/x-canon-cr2" />
                <data android:mimeType="image/x-nikon-nef" />
                <data android:mimeType="image/x-sony-arw" />
            </intent-filter>

            <!-- "Open with" from a file manager -->
            <intent-filter android:label="@string/share_label">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/fits" />
                <data android:mimeType="image/fits" />
                <data android:mimeType="application/x-fits" />
                <data android:mimeType="image/x-adobe-dng" />
            </intent-filter>
        </activity>
    </application>
</manifest>
